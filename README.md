# GitHub Followers Chart (Async JavaScript)

This project demonstrates how to fetch GitHub user follower data asynchronously and visualize it in a chart using **Vanilla JavaScript**.  
It focuses on **core JavaScript fundamentals** rather than frameworks.

---

## 🚀 Features

- Fetches GitHub user followers using the GitHub REST API
- Uses `async/await` for asynchronous programming
- Dynamically updates the DOM
- Displays follower data in a chart
- No frameworks (pure JavaScript)

---

## 🛠️ Technologies Used

- HTML5
- CSS3
- Vanilla JavaScript (ES6+)
- GitHub REST API
- Chart library (e.g., Chart.js)

---

## 📊 How It Works

1. User enters a GitHub username
2. JavaScript fetches follower data asynchronously
3. Data is processed and extracted
4. DOM is updated dynamically
5. Followers are visualized in a chart

---



